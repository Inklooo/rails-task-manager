<%= @task.title %>

<button class="btn btn-outline-primary"><%=link_to "Edit task",  edit_task_path %></button>

<button class="btn btn-outline-danger">
  <%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></button>
